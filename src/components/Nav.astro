---
import { Terminal } from "@lucide/astro";
import { Button } from "@/components/starwind/button";
const navItems = [
    { name: "Projects", href: "/projects" },
    { name: "About", href: "/about" },
    { name: "Contact", href: "/contact" },
];
---

<nav
    class="grid grid-cols-2 grid-rows-[auto_auto_auto] max-w-6xl mx-auto gap-6"
>
    <div
        class="col-span-2 md:col-span-1 flex justify-center items-center gap-4"
    >
        <a
            href="/"
            class="flex items-center gap-4 p-4 hover:bg-black hover:border-black hover:rounded hover:text-green-300"
        >
            <span
                class="inline-flex items-center justify-center bg-black rounded p-2"
            >
                <Terminal class="h-8 w-8 md:h-14 md:w-20 text-white" />
            </span>
            <span class="flex flex-col">
                <span class="font-bold text-2xl md:text-6xl">Cody Venditto</span
                >
                <p
                    class="hidden md:block text-gray-400 text-md md:text-2xl p-2"
                >
                    Aspiring Web Developer
                </p>
            </span>
        </a>
    </div>
    <div
        class="col-start-1 row-start-2 col-span-2 md:row-start-1 md:col-start-2 md:col-span-1 flex items-center md:justify-end justify-center"
    >
        <ul class="flex items-center gap-8 text-md md:text-xl">
            {
                navItems.map((item) => {
                    if (item.name === "Contact") {
                        return (
                            <li class="hover:text-blue-300">
                                <a href={item.href}>
                                    <Button
                                        class="bg-white text-black border border-black hover:bg-black hover:text-white transition-colors text-md"
                                        size="md"
                                    >
                                        {item.name}
                                    </Button>
                                </a>
                            </li>
                        );
                    } else {
                        return (
                            <li class="hover:text-gray-400">
                                <a href={item.href}>{item.name}</a>
                            </li>
                        );
                    }
                })
            }
        </ul>
    </div>
    <div class="md:row-start-2 row-start-3 col-span-2">
        <hr class="w-full" />
    </div>
</nav>
